function Block(letter, rate, used, father, code) { this.letter = letter; this.rate = rate; this.used = used; this.father = father; this.code = code; 

1} 

Ifunction FindCode(block) { if (tree[block.father].code != *') { block.code = tree[block.father].code + '1*; 

} 

else { if (block.letter -= tree[mlnlndex].letter) { block.code - '0'; else if (block.letter •• treetpreninlndex].letter) { block.code - •!•; else { 

FindCode(tree[block.father]); block.code "= tree[block.father].code + '0*;
